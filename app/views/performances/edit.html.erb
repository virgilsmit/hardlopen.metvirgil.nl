<div class="zones-page surface-dark">
  <h1 class="page-heading">Prestatie bewerken</h1>

  <div class="card-surface">
    <%= render "form", performance: @performance %>
  </div>

  <div style="margin-top: 1.5rem; display: flex; gap: 1rem;">
    <% if request.referer&.include?('zones') %>
      <%= link_to "← Terug naar Zones", user_zones_path(@performance.user_id), class: 'theme-btn track-btn--outline' %>
    <% else %>
      <%= link_to "← Terug naar Profiel", user_path(@performance.user_id), class: 'theme-btn track-btn--outline' %>
    <% end %>
  </div>
</div>
