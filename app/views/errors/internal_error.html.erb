<div class="container py-5">
  <div class="row justify-content-center">
    <div class="col-md-8 col-lg-6">
      <div style="background: linear-gradient(135deg, rgba(220, 53, 69, 0.1) 0%, rgba(220, 53, 69, 0.05) 100%); border: 2px solid rgba(220, 53, 69, 0.3); padding: 3rem; border-radius: 20px; text-align: center; box-shadow: 0 10px 40px rgba(0, 0, 0, 0.3);">
        <!-- Error Icon -->
        <div style="font-size: 5rem; margin-bottom: 1.5rem; animation: shake 0.5s;">⚠️</div>
        
        <!-- Titel -->
        <h1 style="color: #dc3545; font-weight: 700; margin-bottom: 1rem; font-size: 2rem;">
          Oeps! Er ging iets mis
        </h1>
        
        <!-- Subtitel -->
        <p style="color: rgba(244, 248, 255, 0.8); font-size: 1.1rem; margin-bottom: 2rem;">
          Er is een onverwachte fout opgetreden. Onze excuses voor het ongemak.
        </p>
        
        <!-- Foutcode Card -->
        <div style="background: rgba(0, 0, 0, 0.4); padding: 2rem; border-radius: 12px; margin-bottom: 2rem; border: 1px solid rgba(255, 255, 255, 0.1);">
          <div style="color: rgba(244, 248, 255, 0.7); font-size: 0.9rem; margin-bottom: 0.5rem;">
            Foutcode voor de beheerder:
          </div>
          <code style="background: rgba(252, 76, 2, 0.2); color: #FC4C02; padding: 0.75rem 1.5rem; border-radius: 8px; font-size: 1.5rem; font-weight: 700; display: inline-block; letter-spacing: 2px;">
            <%= @error_code %>
          </code>
        </div>
        
        <!-- Instructies -->
        <div style="background: rgba(30, 227, 207, 0.1); border: 1px solid rgba(30, 227, 207, 0.3); padding: 1.5rem; border-radius: 12px; margin-bottom: 2rem; text-align: left;">
          <h5 style="color: #1ee3cf; font-weight: 700; margin-bottom: 1rem;">📋 Wat nu?</h5>
          <ul style="color: rgba(244, 248, 255, 0.8); margin: 0; padding-left: 1.5rem;">
            <li style="margin-bottom: 0.5rem;">Probeer de pagina opnieuw te laden</li>
            <li style="margin-bottom: 0.5rem;">Ga terug naar de vorige pagina</li>
            <li style="margin-bottom: 0.5rem;">Neem contact op met de beheerder en geef de foutcode <strong style="color: #FC4C02;"><%= @error_code %></strong> door</li>
          </ul>
        </div>
        
        <!-- Actie Knoppen -->
        <div class="d-flex flex-column gap-3">
          <%= link_to '← Terug naar Home', root_path, 
            class: 'btn btn-lg',
            style: 'background: linear-gradient(135deg, #FC4C02 0%, #FF6B35 100%); color: white; border: none; font-weight: 600; padding: 1rem;'
          %>
          
          <a href="javascript:history.back()" class="btn btn-lg" style="background: rgba(255, 255, 255, 0.1); color: #f4f8ff; border: 1px solid rgba(255, 255, 255, 0.2); font-weight: 600; padding: 1rem;">
            Vorige Pagina
          </a>
          
          <a href="javascript:window.location.reload()" class="btn btn-lg" style="background: rgba(30, 227, 207, 0.1); color: #1ee3cf; border: 1px solid rgba(30, 227, 207, 0.3); font-weight: 600; padding: 1rem;">
            🔄 Probeer Opnieuw
          </a>
        </div>
        
        <!-- Contact Info -->
        <div style="margin-top: 2rem; padding-top: 2rem; border-top: 1px solid rgba(255, 255, 255, 0.1);">
          <p style="color: rgba(244, 248, 255, 0.6); font-size: 0.9rem; margin: 0;">
            <% if logged_in? && current_user.admin? %>
              <a href="<%= admin_error_logs_path %>" style="color: #1ee3cf; text-decoration: none;">
                → Bekijk Error Logs in Admin Panel
              </a>
            <% else %>
              Blijft deze fout zich voordoen? Neem contact op met de beheerder.
            <% end %>
          </p>
        </div>
      </div>
    </div>
  </div>
</div>

<style>
  @keyframes shake {
    0%, 100% { transform: translateX(0); }
    25% { transform: translateX(-10px) rotate(-5deg); }
    75% { transform: translateX(10px) rotate(5deg); }
  }
</style>

